export enum FeedbackCategory {
  GENERAL = 'general',
  BUG = 'bug',
  FEATURE = 'feature',
  OTHER = 'other',
}

export enum FeedbackStatus {
  NEW = 'new',
  REVIEWED = 'reviewed',
  RESOLVED = 'resolved',
}
